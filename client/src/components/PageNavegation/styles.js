import Styled from 'styled-components'

export const Container = Styled.div`
    width: 100%;
    margin: 0 auto;
    display: flex;
    justify-content: flex-end;
    align-items: end;

    & a {
        margin: 0;
        padding: .5rem;
    }
`